{
  "name": "saas-inheif-beer",
  "private": true,
  "description": "Monorepo: FastAPI at repo root. The Next.js app lives in frontend/ — the Vercel project named \"frontend\" MUST use Root Directory \"frontend\" or builds fail with \"No Next.js version detected\". See VERCEL_FRONTEND_ROOT.md",
  "devDependencies": {
    "@playwright/test": "^1.48.2",
    "vercel": "^50.33.1"
  },
  "scripts": {
    "test": "playwright test",
    "deploy": "python scripts/deploy_vercel.py",
    "deploy:prod": "python scripts/deploy_vercel.py --prod",
    "deploy:backend": "python scripts/deploy_vercel.py --backend-only",
    "deploy:backend:prod": "python scripts/deploy_vercel.py --backend-only --prod",
    "deploy:frontend": "python scripts/deploy_vercel.py --frontend-only",
    "deploy:frontend:prod": "python scripts/deploy_vercel.py --frontend-only --prod",
    "vercel": "npx vercel",
    "vercel:login": "npx vercel login"
  }
}
